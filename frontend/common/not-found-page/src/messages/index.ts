import { defineMessages } from 'react-intl'

export const namespace = '@client-starter/not-found-page'

export default defineMessages({
  pageNotFound: {
    id: `${namespace}.page_not_found`,
    defaultMessage: 'Страница не найдена',
  },
})
